/**
 * @param {number[]} nums
 * @return {number}
 */
var smallestEqual = function(nums) {
    let num = -1;

        for(let i=0;i<nums.length;i++){
            if(i%10==nums[i]){
                num = i;
                break;
            }
        }
        return num;
};
